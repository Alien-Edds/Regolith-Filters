# Parameters

### load: boolean
if true, loads files from the cache.

### deleteFolders: boolean
if true, deletes existing folders before loading the cache.

### excludeItems: string[]
excludes folders or files based on if they include one of these strings in their path.